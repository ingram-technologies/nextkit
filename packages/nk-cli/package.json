{
	"name": "@ingram-tech/nk-cli",
	"version": "0.3.0",
	"description": "nk — the nextkit CLI. One command for dev, formatting (oxlint + oxfmt for code, Prettier for SQL), and lint/check/type-check/build across Ingram Next.js sites.",
	"license": "MIT",
	"type": "module",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/ingram-technologies/nextkit.git",
		"directory": "packages/nk-cli"
	},
	"publishConfig": {
		"access": "public"
	},
	"files": [
		"bin",
		"lib"
	],
	"bin": {
		"nk": "bin/nk.js"
	},
	"scripts": {
		"build": "true",
		"type-check": "true",
		"test": "vitest run"
	},
	"dependencies": {
		"prettier": "^3.8.3",
		"prettier-plugin-sql": "^0.20.0"
	},
	"devDependencies": {
		"vitest": "^4.1.6"
	},
	"engines": {
		"node": ">=20"
	}
}
